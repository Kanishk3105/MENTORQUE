# Database (Neon or Supabase Postgres connection string)
DATABASE_URL=postgresql://neondb_owner:npg_dXxRhAsCLr57@ep-lingering-haze-aznddo80.c-3.ap-southeast-1.aws.neon.tech/neondb?sslmode=require

# JWT (single secret — no platform SSO, no OAuth)
JWT_SECRET=mentorque_super_secret_2026_@12345
JWT_EXPIRES_IN=7d

# Server
PORT=5001
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Seed script — admin account credentials
ADMIN_EMAIL=admin@mentorque.dev
ADMIN_PASSWORD=admin1234
ADMIN_NAME=Admin User
# Password used for all seeded mentors/users
SEED_PASSWORD=password123

# --- AI recommendation engine (all optional — falls back to deterministic
# scoring / hashed embeddings if not set, so the app works with zero keys) ---
HF_API_KEY=
HF_EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
